//Header file of mem_alloc.c file
int* node_maker(int );// to make the array.
void insert_atPos(int, int *);// to insert the element at position.
void delete_at_pos(int*, int *);// to delete the element at position.
